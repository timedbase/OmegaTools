{
  "name": "contracts",
  "version": "1.0.0",
  "description": "OmegaTools Smart Contracts",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:gas": "forge test --gas-report",
    "coverage": "forge coverage",
    "clean": "forge clean && rm -rf node_modules",
    "deploy:monad": "forge script script/Deploy.s.sol:Deploy --rpc-url monad --broadcast --verify",
    "format": "forge fmt",
    "snapshot": "forge snapshot"
  }
}
